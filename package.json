{
  "name": "interactive-simulator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "prisma generate",
    "prestart": "node scripts/audit.js",
    "start": "npx prisma db push --accept-data-loss --skip-generate && node server.js",
    "audit": "node scripts/audit.js",
    "db:studio": "npx prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "prisma": "^5.22.0"
  }
}
